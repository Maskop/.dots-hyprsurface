plugin {
 touch_gestures {
  sensitivity = 4.0

  # must be >= 3
  workspace_swipe_fingers = 3

  # switching workspaces by swiping from an edge, this is separate from workspace_swipe_fingers
  # and can be used at the same time
  # possible values: l, r, u, or d
  # to disable it set it to anything else
  workspace_swipe_edge = u

  # in milliseconds
  long_press_delay = 400

  # resize windows by long-pressing on window borders and gaps.
  # If general:resize_on_border is enabled, general:extend_border_grab_area is
  # used for floating windows
  resize_on_border_long_press = true

  # in pixels, the distance from the edge that is considered an edge
  edge_margin = 10
 }
 hyprbars {
  enabled = true
  bar_height = 20
  bar_text_font = "JetBrainsMonoNL NF, 10"
  bar_color = rgba(000000CC)
  bar_part_of_window = true
  bar_blur = true

  hyprbars-button = rgb(ff4040), 15, 󰖭, hyprctl dispatch killactive
  hyprbars-button = rgb(eeee11), 15, , hyprctl dispatch fullscreen 1
  hyprbars-button = rgb(40ff40), 15, 󱂬, hyprctl dispatch togglefloating

  # cmd to run on double click of the bar
  on_double_click = hyprctl dispatch fullscreen 1
 }
}
